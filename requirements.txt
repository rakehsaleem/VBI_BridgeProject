# Core dependencies
numpy>=1.21.0
scipy>=1.7.0
matplotlib>=3.5.0
scikit-learn>=1.0.0

# TensorFlow with GPU support
# For GPU support, install in this order:
# 1. Install CUDA Toolkit (https://developer.nvidia.com/cuda-downloads)
# 2. Install cuDNN (https://developer.nvidia.com/cudnn)
# 3. Then: pip install tensorflow[and-cuda]
tensorflow>=2.13.0

